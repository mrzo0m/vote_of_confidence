spring:
  application:
    name: discovery
  profiles:
    active: native
server:
  port: 8761
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
logging:
  level:
    com:
      netflix:
        eureka: info
        discovery: info