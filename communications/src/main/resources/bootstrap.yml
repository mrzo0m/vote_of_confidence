spring:
  application:
    name: communications
  profiles:
    active: native
  cloud:
    config:
      discovery:
        enabled: true
logging:
  level:
    com:
      netflix:
        eureka: trace
        discovery: trace