spring:
  application:
    name: communications
  profiles:
    active: native
  cloud:
    config:
      discovery:
        enabled: true
logging:
  level:
    com:
      netflix:
        eureka: info
        discovery: info